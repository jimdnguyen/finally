---
phase: 01-database-core-api
plan: 04
type: execute
wave: 3
depends_on: [01]
files_modified:
  - backend/app/health/routes.py
  - backend/app/health/__init__.py
  - backend/tests/test_health.py
autonomous: true
requirements: [SYS-01]
user_setup: []

must_haves:
  truths:
    - "GET /api/health returns 200 with status information"
    - "Health check confirms database is accessible"
  artifacts:
    - path: backend/app/health/routes.py
      provides: "GET /api/health endpoint returning status"
      exports: ["create_health_router()"]
    - path: backend/tests/test_health.py
      provides: "Unit test verifying health check response"
  key_links:
    - from: "backend/app/health/routes.py"
      to: "database"
      via: "SELECT 1 query"
      pattern: "SELECT 1"
---

<objective>
Implement the `GET /api/health` endpoint for Docker healthcheck and deployment readiness confirmation.

Purpose: Enable Docker to verify container is running and database is accessible. Used in deployment scripts and orchestration.

Output: Simple health check endpoint that confirms database connectivity and returns status JSON.
</objective>

<execution_context>
@D:\Projects\Udemy\AI Coder Vibe Coder to Agentic Engineer in 3 Weeks\finally\.claude\get-shit-done\workflows\execute-plan.md
@D:\Projects\Udemy\AI Coder Vibe Coder to Agentic Engineer in 3 Weeks\finally\.planning\phases\01-database-core-api\01-01-PLAN.md

Database access via get_db() dependency from 01-01-PLAN.md.
</execution_context>

<context>
@D:\Projects\Udemy\AI Coder Vibe Coder to Agentic Engineer in 3 Weeks\finally\.planning\PLAN.md
@D:\Projects\Udemy\AI Coder Vibe Coder to Agentic Engineer in 3 Weeks\finally\.planning\ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create health check router</name>
  <files>backend/app/health/routes.py</files>
  <action>
Create `backend/app/health/routes.py` with FastAPI router:

1. Factory function `create_health_router()` → APIRouter
   - Returns an APIRouter for later mounting

2. GET `/api/health` endpoint
   - Signature: `async def health_check(db: Connection = Depends(get_db)) -> dict`
   - Query database: `SELECT 1` (minimal connectivity test)
   - Response: JSON dict with structure:
     ```json
     {
       "status": "healthy",
       "database": "connected",
       "timestamp": "2026-04-09T12:34:56Z"
     }
     ```
   - Response status: 200
   - On database error: response status 503 (Service Unavailable) with status="unhealthy", database="disconnected"

3. Error handling
   - Wrap database query in try/except
   - On exception (e.g., database locked): return 503 with database="error"
   - Log exception at error level but do not re-raise

Include docstring explaining health check purpose and response schema.
  </action>
  <verify>
    <automated>grep -n "def create_health_router\|async def health_check\|SELECT 1\|status.*healthy\|database.*connected" backend/app/health/routes.py | wc -l</automated>
  </verify>
  <done>routes.py exists with create_health_router factory and GET /api/health endpoint, endpoint queries database and returns status JSON</done>
</task>

<task type="auto">
  <name>Task 2: Create health package init</name>
  <files>backend/app/health/__init__.py</files>
  <action>
Create `backend/app/health/__init__.py` as a simple public API export:

```python
from .routes import create_health_router

__all__ = ["create_health_router"]
```

No docstring needed for __init__.py.
  </action>
  <verify>
    <automated>grep -n "from .routes import\|__all__" backend/app/health/__init__.py | wc -l</automated>
  </verify>
  <done>__init__.py exists with proper export from routes</done>
</task>

<task type="auto">
  <name>Task 3: Write unit test for health endpoint</name>
  <files>backend/tests/test_health.py</files>
  <action>
Create `backend/tests/test_health.py` with pytest test function:

1. Test function: `test_health_check(client)`
   - Call `client.get("/api/health")`
   - Assert status 200
   - Assert response JSON has "status": "healthy"
   - Assert response JSON has "database": "connected"
   - Assert response JSON has "timestamp" key (ISO format timestamp)

2. Optional test: `test_health_check_database_error(client, test_db, monkeypatch)`
   - Mock database to raise an exception
   - Call `client.get("/api/health")`
   - Assert status 503
   - Assert response JSON has "database": "error" or "disconnected"

Include docstring explaining test purpose. If the second test is complex, focus on the first one.

Use conftest.py fixtures (client from 01-01-PLAN.md).
  </action>
  <verify>
    <automated>grep -n "def test_health_check\|assert.*healthy\|assert.*connected" backend/tests/test_health.py | wc -l</automated>
  </verify>
  <done>test_health.py exists with test_health_check function, asserts on response status and JSON schema</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public endpoint | Health check is intentionally public (no auth); returns only status, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | GET /api/health response | accept | Health check intentionally public; returns only generic status and timestamp; no sensitive data |
| T-01-04-02 | Denial of Service | Health check database query | accept | Single SELECT 1 query is O(1); no joins, minimal resource usage |

</threat_model>

<verification>
After Plan 04 execution:
- Health endpoint returns 200 with valid JSON schema when database is healthy
- Health endpoint returns 503 with "disconnected" status when database is unavailable
- No database errors or connection leaks
- Test passes with 100% coverage of health/routes.py
</verification>

<success_criteria>
1. Endpoint responds with 200 status and valid JSON schema
2. Response includes "status", "database", "timestamp" keys
3. Database error handling returns 503 with appropriate status
4. Unit test passes without errors
5. Endpoint can be used in Docker HEALTHCHECK instruction (simple HTTP GET, checks status code)
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-core-api/01-04-SUMMARY.md`
</output>
