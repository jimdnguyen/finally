---
phase: 01-database-core-api
plan: 03
type: execute
wave: 3
depends_on: [01]
files_modified:
  - backend/app/watchlist/models.py
  - backend/app/watchlist/routes.py
  - backend/app/watchlist/__init__.py
  - backend/tests/test_watchlist.py
autonomous: true
requirements: [WTCH-01]
user_setup: []

must_haves:
  truths:
    - "GET /api/watchlist returns all watched tickers with live prices from PriceCache"
    - "Watchlist endpoint uses synchronized database access (no race conditions during queries)"
    - "Price data is live (current price, previous price, change direction from PriceCache)"
  artifacts:
    - path: backend/app/watchlist/models.py
      provides: "WatchlistItemResponse Pydantic model with ticker and price fields"
      exports: ["WatchlistItemResponse", "WatchlistResponse"]
    - path: backend/app/watchlist/routes.py
      provides: "GET /api/watchlist endpoint returning tickers with live prices"
      exports: ["create_watchlist_router()"]
    - path: backend/tests/test_watchlist.py
      provides: "Unit test verifying watchlist endpoint returns correct format and live prices"
  key_links:
    - from: "backend/app/watchlist/routes.py"
      to: "PriceCache"
      via: "dependency injection (get_price_cache)"
      pattern: "price_cache.get_latest"
    - from: "backend/app/watchlist/routes.py"
      to: "database watchlist table"
      via: "SELECT query"
      pattern: "SELECT ticker FROM watchlist"
---

<objective>
Implement the `GET /api/watchlist` endpoint that returns all tickers the user is watching with their live market prices.

Purpose: Enable frontend to display watched tickers with current and previous prices for display in the watchlist panel.

Output: Working watchlist endpoint returning live prices, integrated with existing price cache and database layer.
</objective>

<execution_context>
@D:\Projects\Udemy\AI Coder Vibe Coder to Agentic Engineer in 3 Weeks\finally\.claude\get-shit-done\workflows\execute-plan.md
@D:\Projects\Udemy\AI Coder Vibe Coder to Agentic Engineer in 3 Weeks\finally\.planning\phases\01-database-core-api\01-01-PLAN.md

Key interfaces from 01-01-PLAN.md:
- PriceCache.get_latest(ticker: str) → PriceUpdate
- PriceUpdate has: price (float), previous_price (float), direction (str: "up"/"down"/"flat")
- Database connection via get_db() dependency
</execution_context>

<context>
@D:\Projects\Udemy\AI Coder Vibe Coder to Agentic Engineer in 3 Weeks\finally\.planning\PLAN.md
@D:\Projects\Udemy\AI Coder Vibe Coder to Agentic Engineer in 3 Weeks\finally\.planning\ROADMAP.md
@D:\Projects\Udemy\AI Coder Vibe Coder to Agentic Engineer in 3 Weeks\finally\.planning\phases\01-database-core-api\01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create watchlist models (Pydantic schemas)</name>
  <files>backend/app/watchlist/models.py</files>
  <action>
Create `backend/app/watchlist/models.py` with Pydantic v2 BaseModel classes:

1. `WatchlistItemResponse` — represents a single ticker with live price
   - `ticker`: str (e.g., "AAPL")
   - `price`: float (current price from PriceCache)
   - `previous_price`: float (price at last update)
   - `direction`: str (literal "up" | "down" | "flat")
   - `change_amount`: float (price - previous_price, calculated field)

2. `WatchlistResponse` — list of items
   - `watchlist`: list[WatchlistItemResponse]

Validate ticker format (alphanumeric, 1-5 chars, uppercase only). Do NOT validate against external data — only format.

Include docstrings per project conventions (one-line summary, no defensive comments).
  </action>
  <verify>
    <automated>grep -n "class WatchlistItemResponse\|class WatchlistResponse\|ticker: str\|price: float" backend/app/watchlist/models.py | wc -l</automated>
  </verify>
  <done>models.py exists with WatchlistItemResponse and WatchlistResponse classes, all fields properly typed, docstrings present</done>
</task>

<task type="auto">
  <name>Task 2: Create watchlist router with GET endpoint</name>
  <files>backend/app/watchlist/routes.py</files>
  <action>
Create `backend/app/watchlist/routes.py` with FastAPI router:

1. Factory function `create_watchlist_router()` → APIRouter
   - No dependencies — returns an APIRouter for later mounting

2. GET `/api/watchlist` endpoint
   - Signature: `async def get_watchlist(db: Connection = Depends(get_db), price_cache: PriceCache = Depends(get_price_cache))`
   - Query database: `SELECT ticker FROM watchlist WHERE user_id = ? ORDER BY added_at DESC`
   - For each ticker, call `price_cache.get_latest(ticker)` to fetch live prices
   - If ticker not in price_cache (edge case), use fallback: price=0.0, previous_price=0.0, direction="flat"
   - Build list of WatchlistItemResponse objects
   - Return WatchlistResponse with the list
   - Response status: 200
   - Response model: WatchlistResponse

3. Error handling
   - No try/catch needed; database errors will bubble as 500

Include docstring on function explaining purpose and response format. No extra features (no POST/DELETE here — defer to later phase).
  </action>
  <verify>
    <automated>grep -n "def create_watchlist_router\|async def get_watchlist\|SELECT ticker FROM watchlist\|price_cache.get_latest" backend/app/watchlist/routes.py | wc -l</automated>
  </verify>
  <done>routes.py exists with create_watchlist_router factory and GET endpoint, endpoint queries database and price cache, returns WatchlistResponse</done>
</task>

<task type="auto">
  <name>Task 3: Create watchlist package init</name>
  <files>backend/app/watchlist/__init__.py</files>
  <action>
Create `backend/app/watchlist/__init__.py` as a simple public API export:

```python
from .models import WatchlistItemResponse, WatchlistResponse
from .routes import create_watchlist_router

__all__ = ["WatchlistItemResponse", "WatchlistResponse", "create_watchlist_router"]
```

No docstring needed for __init__.py.
  </action>
  <verify>
    <automated>grep -n "from .models import\|from .routes import\|__all__" backend/app/watchlist/__init__.py | wc -l</automated>
  </verify>
  <done>__init__.py exists with proper exports from models and routes</done>
</task>

<task type="auto">
  <name>Task 4: Write unit test for watchlist endpoint</name>
  <files>backend/tests/test_watchlist.py</files>
  <action>
Create `backend/tests/test_watchlist.py` with pytest test function:

1. Test fixture (uses conftest.py fixtures from 01-01-PLAN.md):
   - Import `client: TestClient`, `test_db: Connection`, `price_cache: PriceCache` from conftest
   - Insert 3 test tickers into the watchlist table: AAPL, GOOGL, TSLA
   - Seed prices in price_cache: AAPL (price=190.5, previous=189.0), GOOGL (price=175.0, previous=176.0), TSLA (price=242.0, previous=242.0)

2. Test function: `test_get_watchlist(client, test_db, price_cache)`
   - Call `client.get("/api/watchlist")`
   - Assert status 200
   - Assert response JSON has "watchlist" key with 3 items
   - Assert each item has: ticker, price, previous_price, direction, change_amount
   - Assert AAPL has direction="up" (price > previous), GOOGL has direction="down"
   - Assert change_amount is correct (price - previous_price)

Do NOT test database persistence or multi-user isolation — defer to integration tests. This is a unit test of the endpoint contract.

Include docstring explaining test purpose.
  </action>
  <verify>
    <automated>grep -n "def test_get_watchlist\|assert.*watchlist\|assert.*direction" backend/tests/test_watchlist.py | wc -l</automated>
  </verify>
  <done>test_watchlist.py exists with test_get_watchlist function, asserts on response shape and price calculations</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database → Price Cache | Tickers from DB may not exist in PriceCache (edge case: ticker added to watchlist but not yet streamed); mitigated with fallback (0.0, direction="flat") |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | GET /api/watchlist endpoint | accept | Single-user model; hardcoded user_id="default"; no sensitive data beyond price (public market data) |
| T-01-03-02 | Denial of Service | Watchlist query on large dataset | accept | Default seeding limits watchlist to 10 tickers; query is O(n) with no joins; acceptable performance |
| T-01-03-03 | Elevation of Privilege | user_id injection via price_cache | accept | price_cache is in-memory; no user context; tickers are public |

</threat_model>

<verification>
After Plan 03 execution:
- Watchlist endpoint returns JSON with correct structure (watchlist array of objects with ticker, price, previous_price, direction, change_amount)
- Price data reflects PriceCache state (live, not stale)
- No database errors or connection leaks
- Test passes with 100% coverage of watchlist/routes.py and watchlist/models.py
</verification>

<success_criteria>
1. Endpoint responds with 200 status and valid JSON schema
2. All default 10 tickers appear in response with live prices from PriceCache
3. Price flash direction (up/down/flat) matches actual price movement
4. change_amount is calculated correctly (price - previous_price)
5. Unit test passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-core-api/01-03-SUMMARY.md`
</output>
